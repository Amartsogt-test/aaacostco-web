name: Bank Rates Checker

on:
  schedule:
    - cron: '*/30 * * * *'  # Run every 30 minutes
  workflow_dispatch:        # Allow manual trigger

jobs:
  check-rates:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Create Service Account File
        run: |
          mkdir -p functions
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' | base64 -d > functions/service-account.json
        # Secret must be Base64 encoded to avoid JSON syntax errors in shell

      - name: Install Dependencies
        run: npm ci

      - name: Run Rates Script
        run: npm run core:bank-rates
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ./functions/service-account.json
